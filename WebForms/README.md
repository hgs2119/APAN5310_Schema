# apan5310 flask forms template

PLEASE NOTE: Flask forms will only run as intended if copied out of this subfolder (currently WebForms) and into their own repository to preserve filepaths etc. as intended.

Flask, SQLAlchemy and web forms

Initialize database (first time only)

```
flask db init
flask db migrate -m 'initialize database'
flask db upgrade
```
